package sortPerformanceAnalysis;

public class insertionSortObject <T extends Number>
{	
	public T[] sortUsingInsertion(T[] array, int nums)
	{
		int i=1, j=0;
		//If the array contains only one item.
		if(nums==0)
			return array;
		for(i;i<nums;i)
		
		return array;
	}
}
